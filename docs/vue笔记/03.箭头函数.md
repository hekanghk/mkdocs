#### 箭头函数

```js
//1. 普通方式
const aaa = function(){
  
}
//2. 在对象中定义函数
const obj = {
  aaa(){
    
  }
}
//3. 箭头函数
const aaa = (参数列表) => {
  
} 
//只有一行执行代码的箭头函数
const mul = (num1, num2) => num1 * num2
```